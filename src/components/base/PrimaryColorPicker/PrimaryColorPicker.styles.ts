import { createStyles } from '@mantine/core';

const PrimaryColorPickerStyles = createStyles(() => ({
  body: {
    marginTop: 10,
    padding: 30,
  },
}));

export default PrimaryColorPickerStyles;
